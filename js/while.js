(function() {
    "use strict";

    function displayDoubleTwo() {
        var output = "";
        var i = 2;
        while (i <= 65536) {
            output += i + "\n";
            i *= 2;
        }
        return output;
    }

console.log(displayDoubleTwo());
function addSorNot(num) {
    return (num === 1) ? "" : "s";
}
function createTransactionMsg(conesSold, orderedCones, inventoryCones)
    var allCones = Math.floor(Math.random() * 50) + 50;
    var requestedCones = Math.floor(Math.random() * 5) + 1;


    do {
        console.log("We have this many cones today: "  + allCones);


    } while()
})();
